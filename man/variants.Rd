% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/representation.R
\name{variants}
\alias{variants}
\title{Declare Variants of a Representation}
\usage{
variants(.f = NULL, ..., .name = NULL)
}
\arguments{
\item{.f}{The canonical variant of the representation: an ordinary
function, evaluated the way the matching \code{eval_} function documents.
\code{NULL} if the distribution can only provide the variants named in \code{...}.}

\item{...}{Named functions, one per variant, named by the \emph{level} of the
variant they provide (\verb{right = }, \verb{strict = }). Which levels are available
depends on the representation --- they are the values accepted by the
variant arguments of the matching \code{eval_} function, such as the \code{side}
argument of \code{\link[=eval_quantile]{eval_quantile()}}.}

\item{.name}{The name of the representation being built, such as
\code{"quantile"}. Rarely needed: \code{\link[=distribution]{distribution()}} fills this in from the
argument the representation is assigned to, which is also when the declared
levels are checked. Supply it to build a representation on its own, away
from a \code{distribution()} call.}
}
\value{
A representation object. It is also a function --- the canonical
variant --- so anything that can use a plain representation can use this.
}
\description{
Bundle a representation function together with alternative \emph{variants} of
it, for use in \code{\link[=distribution]{distribution()}}. A plain function supplied to
\code{distribution()} promises one thing: the representation in its canonical
variant. \code{variants()} is how a distribution promises more --- a right
inverse of the CDF, say, alongside the usual left one.
}
\details{
A variant that is \emph{not} declared here is not unavailable, only underived:
distionary works it out from the distribution's other representations, the
same way it works out a quantile function from a CDF. Declaring one says
"here is a better route than the one you would find yourself", which is
worth doing when the derivation is slow, imprecise, or impossible.

Not every argument of an \code{eval_} function is a variant that can be declared
here. Some, such as the \code{definition} argument of \code{\link[=eval_density]{eval_density()}}, ask
whether a representation exists for this distribution at all rather than
asking for a different function, and are settled before any representation
is consulted. Declaring one is an error, since nothing would ever call it.
}
\examples{
# The Poisson quantile function is the left inverse of its CDF. The
# right inverse steps to the next atom where a probability lands on
# top of a jump, and agrees with the left one everywhere else.
d <- distribution(
  cdf = function(x) ppois(x, lambda = 5),
  pmf = function(x) dpois(x, lambda = 5),
  quantile = variants(
    function(p) qpois(p, lambda = 5),
    right = function(p) {
      q <- qpois(p, lambda = 5)
      q + (ppois(q, lambda = 5) <= p)
    }
  ),
  .support = discrete(natural0()),
  .name = "Poisson"
)
eval_quantile(d, at = c(0.2, 0.5), side = "left")
eval_quantile(d, at = c(0.2, 0.5), side = "right")
}
\seealso{
Other Distribution Construction: 
\code{\link{distribution}()}
}
\concept{Distribution Construction}
