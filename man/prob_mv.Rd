% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prob_mv.R
\name{prob_mv}
\alias{prob_mv}
\alias{prob_bi}
\title{Probability Using Inequalities}
\usage{
prob_mv(distribution, l, ineq)

prob_bi(distribution, x, y, ineq)
}
\arguments{
\item{distribution}{A distribution.}

\item{l}{For \verb{eval_mv_*()}, a list of vectors, one for each variable. A data
frame is such a list, so the output of \code{\link[=realise]{realise()}} can be passed straight
in. If the list is named, its names are matched to the distribution's
\code{\link[=variables]{variables()}}; if not, the vectors are taken in order.}

\item{ineq}{The inequality for each variable: \code{"<="}, \code{"<"}, \code{">"}, or
\code{">="}, read as "variable \code{ineq} value". A single one applies to every
variable; otherwise give one per variable, named to match
\code{\link[=variables]{variables()}} or in order.}

\item{x, y}{For \verb{eval_bi_*()}, vectors of values of the first and second
variables, in the order given by \code{\link[=variables]{variables()}}.}
}
\value{
A numeric vector, with the common length of the inputs.
}
\description{
The probability that each variable falls on a chosen side of its value:
for example \eqn{P(X \le x, Y > y)}. These are the multivariate
counterparts of \code{\link[=prob_left]{prob_left()}} and \code{\link[=prob_right]{prob_right()}}.
}
\details{
With every inequality \code{"<="}, this is the CDF; with every one \code{">"}, the
survival function. Mixing them covers the rest, such as the probability
that one flow is high while another is low.

Strict and non-strict inequalities differ only where a variable has
atoms, and are treated alike for a variable that is continuous.

The probability is worked out from the CDF (or the survival function) by
inclusion-exclusion, and for a distribution on finitely many points, by
adding up the points that satisfy every inequality.
}
\examples{
d <- dst_bi_norm(mean = c(0, 0), sd = c(1, 1), cor = 0.6)
# Both exceeding 1.
prob_bi(d, x = 1, y = 1, ineq = ">")
# The first at most 0, the second exceeding 0.
prob_bi(d, x = 0, y = 0, ineq = c("<=", ">"))

e <- dst_mv_empirical(list(a = c(1, 2, 2, 3), b = c(1, 1, 2, 2)))
prob_mv(e, list(a = 2, b = 1), ineq = c(a = "<", b = "<="))
prob_mv(e, list(a = 2, b = 1), ineq = c(a = "<=", b = "<="))
}
\seealso{
\code{\link[=eval_mv_cdf]{eval_mv_cdf()}}, \code{\link[=eval_mv_survival]{eval_mv_survival()}}.
}
