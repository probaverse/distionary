% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-algebra.R
\name{support_transform}
\alias{support_transform}
\alias{support_shift}
\alias{support_scale}
\alias{support_reciprocal}
\title{Transform a Support}
\usage{
support_transform(
  support,
  fun,
  inv,
  ...,
  increasing = TRUE,
  domain = c(-Inf, Inf),
  range = c(-Inf, Inf)
)

support_shift(support, by)

support_scale(support, by)

support_reciprocal(support)
}
\arguments{
\item{support}{A support object, or a distribution.}

\item{fun, inv}{The map and its inverse. Both must be vectorised, and \code{fun}
must be strictly monotonic on the support.}

\item{...}{Not used; must be empty. Present so that the arguments below are
matched by name.}

\item{increasing}{Whether \code{fun} is increasing. \code{FALSE} for a decreasing map,
which reverses each interval's endpoints.}

\item{domain, range}{The domain and range of \code{fun}, needed to transform an
atomic part that is described rather than enumerated.}

\item{by}{For \code{support_shift()} and \code{support_scale()}, the amount to shift
or scale by. Scaling by zero is not a monotonic map, and is an error.}
}
\value{
A support object.
}
\description{
Push a support through a strictly monotonic map, giving the support of the
transformed variable.
}
\details{
\code{support_shift()}, \code{support_scale()}, and \code{support_reciprocal()} are the
common cases, and avoid having to supply an inverse, a domain, and a range
by hand.

\code{support_reciprocal()} maps each side of zero separately, since \code{1 / x} is
monotonic on each side but not across the two. A support with an atom at
zero has no reciprocal, and is an error. Zero lying inside a continuous
part is fine: a single point carries no probability there.
}
\examples{
support_shift(continuous(c(0, 1)), by = 5)
support_scale(discrete(natural0()), by = 2)

# A decreasing map reverses the interval.
support_scale(continuous(c(1, 2)), by = -1)

# Reciprocal of a support spanning zero.
support_reciprocal(continuous(c(-2, 4)))

# The general form.
support_transform(
  continuous(c(0, Inf)),
  fun = exp, inv = log,
  domain = c(0, Inf), range = c(1, Inf)
)
}
\seealso{
Other Support algebra: 
\code{\link{support_add_atoms}()},
\code{\link{support_contains}()},
\code{\link{support_restrict}()},
\code{\link{support_union}()}
}
\concept{Support algebra}
