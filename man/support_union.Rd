% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-algebra.R
\name{support_union}
\alias{support_union}
\title{Combine Supports}
\usage{
support_union(...)
}
\arguments{
\item{...}{Supports to combine, or a single list of them. Distributions are
accepted in place of supports. With no arguments, the result is
\code{\link[=empty_support]{empty_support()}}, which is the identity for this operation.}
}
\value{
A support object.
}
\description{
The union of two or more supports: everything covered by any of them.
}
\details{
The atomic parts are unioned as series, and the continuous parts are pooled
and merged back into canonical form, so touching or overlapping intervals
come out as one.

An atom that falls inside another support's continuous part stays an atom.
The two carry different kinds of probability, and one does not absorb the
other.
}
\examples{
support_union(continuous(c(0, 1)), continuous(c(0.5, 3)))
support_union(discrete(c(1, 2)), continuous(c(5, 6)))

# The identity.
support_union()
}
\seealso{
\code{\link[=support_restrict]{support_restrict()}} to cut a support down instead.

Other Support algebra: 
\code{\link{support_add_atoms}()},
\code{\link{support_contains}()},
\code{\link{support_restrict}()},
\code{\link{support_transform}()}
}
\concept{Support algebra}
