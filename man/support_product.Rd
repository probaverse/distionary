% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-multivariate.R
\name{support_product}
\alias{support_product}
\title{The Support of a Multivariate Distribution}
\usage{
support_product(...)
}
\arguments{
\item{...}{Supports, one per variable (or several variables, if the
support is itself multivariate). Name them to name the variables:
\code{support_product(flow = continuous(c(0, Inf)), gauge = discrete(1:3))}.
Unnamed univariate supports get the names \code{x1}, \code{x2}, and so on, by
position.}
}
\value{
A support object (inheriting class \code{"support"}).
}
\description{
A multivariate distribution places probability on points with several
coordinates, one per variable. \code{support_product()} builds its support the
way \code{\link[=expand.grid]{expand.grid()}} builds a data frame: every value one variable can take
is paired with every value each of the others can take.
}
\details{
The variables of a product support are the variables of its pieces, in
order, so a product of a bivariate support and a univariate one has three
variables. A multivariate piece brings its own variable names and cannot be
renamed by naming its argument.

Not every multivariate support is a product. Two things are allowed here
that are not:
\itemize{
\item A finite set of points, built by handing \code{\link[=discrete]{discrete()}} a data frame (or a
matrix) with one column per variable and one row per point. This is the
support of an empirical distribution: the points observed, and not every
pairing of an observed \code{x} with an observed \code{y}.
\item A product with such a set as one of its pieces.
}

Supports that are neither --- a triangle such as \code{y <= x}, or a line such
as \code{x + y = 100} --- cannot be described yet.

The variable type (\code{\link[=vtype]{vtype()}}) of a multivariate support describes the
joint distribution: \code{"continuous"} when every piece is continuous,
\code{"discrete"} when every piece is discrete, and \code{"mixed"} otherwise.
}
\examples{
# The support of a bivariate Normal.
support_product(x = continuous(), y = continuous())

# A count paired with an amount.
support_product(
  storms = discrete(natural0()),
  depth = continuous(c(0, Inf))
)

# Finitely many points, not a product.
discrete(data.frame(x = c(1, 2, 2), y = c(5, 5, 6)))
}
\seealso{
\code{\link[=dimension]{dimension()}} and \code{\link[=variables]{variables()}} to count and name the variables.

Other Support: 
\code{\link{atoms}()},
\code{\link{empty_support}()},
\code{\link{is_support}()},
\code{\link{support}()},
\code{\link{support-construction}}
}
\concept{Support}
