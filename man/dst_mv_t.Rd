% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dst_mv_t.R
\name{dst_mv_t}
\alias{dst_mv_t}
\alias{dst_bi_t}
\title{Multivariate Student t Distribution}
\usage{
dst_mv_t(location, scale, df)

dst_bi_t(location, scale, cor, df)
}
\arguments{
\item{location}{Vector of locations, one per variable. Its names, if any,
name the variables.}

\item{scale}{For \code{dst_mv_t()}, the scale matrix: symmetric and positive
semi-definite, with one row and column per variable (its dimnames, if
any, name the variables). For \code{dst_bi_t()}, the two scales; positive.}

\item{df}{Degrees of freedom; single positive number, not necessarily a
whole one. \code{Inf} gives the multivariate Normal.}

\item{cor}{For \code{dst_bi_t()}, the correlation parameter, strictly between
-1 and 1.}
}
\value{
A distribution with one variable per entry of \code{location}.
}
\description{
Makes a multivariate Student t distribution. \code{dst_mv_t()} takes any
number of variables; \code{dst_bi_t()} is a shortcut for two, specified by
scales and a correlation.
}
\details{
The distribution is that of \eqn{\mu + Z / \sqrt{W / \nu}}, where
\eqn{Z} is multivariate Normal with mean zero and covariance \code{scale},
\eqn{W} is chi-squared with \eqn{\nu} = \code{df} degrees of freedom, and
\eqn{\mu} = \code{location}. Its tails are heavier than the Normal's, and its
variables are dependent even when uncorrelated: an extreme \eqn{W}
pushes all of them out together.

The \code{scale} matrix is not the covariance: that is
\code{scale * df / (df - 2)}, for \code{df > 2}. Likewise, \code{location} is the mean
only for \code{df > 1}.

Variables that are not named get the names \code{x1}, \code{x2}, and so on, except
in \code{dst_bi_t()}, where they are \code{x} and \code{y}.

The marginal distribution of any of the variables (see \code{\link[=marginal]{marginal()}}) is
again t with the same degrees of freedom; one variable gives \code{\link[=dst_t]{dst_t()}}.
Given some of the variables (see the \code{given} argument of
\code{\link[=eval_mv_cdf]{eval_mv_cdf()}}), the rest are t with more degrees of freedom: \code{df} plus
the number of variables given. Both are exact.
\subsection{Singular scale}{

As with \code{\link[=dst_mv_norm]{dst_mv_norm()}}, a scale matrix that is only positive
semi-definite gives a distribution on a lower-dimensional flat, with
\code{\link[=vtype]{vtype()}} \code{"singular"} and no density. This is what slicing needs.
}

\subsection{Computing the CDF}{

The CDF and survival function average multivariate Normal probabilities
over \eqn{W}, which is a one-dimensional integral. This allows any \code{df}
(\pkg{mvtnorm}'s own t probabilities need a whole number), and needs
\pkg{mvtnorm} for the Normal probabilities.
}
}
\examples{
dst_mv_t(
  location = c(a = 0, b = 1, c = 2),
  scale = diag(3),
  df = 4.5
)
d <- dst_bi_t(location = c(0, 0), scale = c(1, 2), cor = 0.5, df = 3)
d
variance(d)
eval_bi_density(d, x = 0, y = 0:2)
marginal(d, "y")
}
