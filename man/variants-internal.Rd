% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variant.R
\name{variants-internal}
\alias{variants-internal}
\title{Variants of a Distributional Representation}
\description{
A \emph{variant} is a version of a representation that answers a different
question about the same distribution: the right inverse of a CDF rather
than the left one, the probability strictly below a value rather than at or
below it. Variants are ordinary arguments of the \code{eval_} functions, and
those arguments are the whole specification --- there is no separate list
of them to consult, and no second place to keep up to date.
}
\details{
An argument of \verb{eval_<entry>()} other than \code{distribution} and \code{at} is a
variant argument. Its levels, and which of them is canonical, are read off
its default in the usual R idiom:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{eval_quantile <- function(distribution, at, ..., side = c("left", "right"))
}\if{html}{\out{</div>}}

\code{side} takes "left" or "right", and "left" is canonical --- the variant a
plain function supplied to \code{\link[=distribution]{distribution()}} is taken to provide.
}
\keyword{internal}
