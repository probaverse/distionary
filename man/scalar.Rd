% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scalar.R
\name{scalar}
\alias{scalar}
\alias{length.dst}
\alias{is.na.dst}
\alias{as.list.dst}
\title{A Distribution has Length 1}
\usage{
\method{length}{dst}(x)

\method{is.na}{dst}(x)

\method{as.list}{dst}(x, ...)
}
\arguments{
\item{x}{A distribution object.}

\item{...}{Not used.}
}
\value{
For \code{length()}, the number 1. For \code{is.na()}, a single logical.
For \code{as.list()}, a list containing the one distribution.
}
\description{
A distribution object is one distribution, so \code{length()} gives 1 and
\code{is.na()} gives a single logical. \code{as.list()} wraps the distribution
in a list of one.
}
\details{
A distribution is built out of a list of its properties --- a CDF, a
density, a mean --- and without these methods base R reports on that list
rather than on the distribution. \code{length()} counted the properties and
\code{is.na()} tested each one, so \code{dst_norm(0, 1)} answered with eleven
\code{FALSE}s. Neither answer was about the distribution.

\code{is.na()} is \code{TRUE} for the Null distribution (\code{\link[=dst_null]{dst_null()}}) and \code{FALSE}
for every other. The Null distribution is the missing value of the
distribution world, so it is the one that \code{is.na()} finds.

Note that the properties are still reachable, and are still what the
object is made of: \code{x[["cdf"]]} and \code{names(x)} are unchanged, and
\code{\link[=eval_property]{eval_property()}} is the supported way to get at them. Only the questions
asked of the distribution \emph{as a whole} now answer about the whole.

To hold several distributions, put them in a list; in a data frame, that
is a list-column. A distribution does not have length beyond one.
}
\examples{
d <- dst_norm(0, 1)
length(d)
is.na(d)

# The Null distribution is the missing one.
is.na(dst_null())

# Several distributions go in a list.
ds <- list(dst_norm(0, 1), dst_null(), dst_pois(3))
vapply(ds, is.na, logical(1))
}
