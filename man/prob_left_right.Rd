% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prob_left_right.R
\name{eval_prob_left}
\alias{eval_prob_left}
\alias{eval_prob_right}
\alias{enframe_prob_left}
\alias{enframe_prob_right}
\title{Probability to the Left or Right of a Number}
\usage{
eval_prob_left(distribution, at, ..., inequality = c("weak", "strict"))

eval_prob_right(distribution, at, ..., inequality = c("strict", "weak"))

enframe_prob_left(
  ...,
  at,
  inequality = "weak",
  arg_name = ".arg",
  fn_prefix = "prob_left",
  sep = "_"
)

enframe_prob_right(
  ...,
  at,
  inequality = "strict",
  arg_name = ".arg",
  fn_prefix = "prob_right",
  sep = "_"
)
}
\arguments{
\item{distribution, ...}{A distribution, or possibly multiple
distributions in the case of \code{...}.}

\item{at}{Vector of values to evaluate the representation at.}

\item{inequality}{Which inequality the probability is taken over. For
\code{eval_prob_left()}, \code{"weak"} is the probability of \code{X <= at} and
\code{"strict"} the probability of \code{X < at}; for \code{eval_prob_right()},
\code{"strict"} is the probability of \code{X > at} and \code{"weak"} the probability of
\code{X >= at}.

The defaults are the ones that make each function its familiar
counterpart --- \code{\link[=eval_cdf]{eval_cdf()}} and \code{\link[=eval_survival]{eval_survival()}} respectively --- and
the choice only makes a difference where \code{at} is an atom.}

\item{arg_name}{For \code{enframe_}, name of the column containing
the function arguments. Length 1 character vector.}

\item{fn_prefix}{For \code{enframe_}, name of the function to
appear in the column(s). Length 1 character vector.}

\item{sep}{When \code{enframe}'ing more than one distribution, the
character that will be separating the \code{fn_name} and the distribution name.
Length 1 character vector.}
}
\value{
The evaluated representation in vector form (for \code{eval_})
with length matching the length of \code{at}, and data frame
or tibble form (for \code{enframe_}) with number of rows matching the
length of \code{at}. The \code{at} input occupies the first column,
named \code{.arg} by default, or the specification in \code{arg_name};
the evaluated representations for each distribution in \code{...}
go in the subsequent columns (one column per distribution). For a
single distribution, this column is named according to the
representation by default (cdf, survival, quantile, etc.),
or the value in \code{fn_prefix}. For multiple distributions, unnamed
distributions are auto-named, and columns are named
\verb{<fn_prefix><sep><distribution_name>} (e.g., \code{cdf_distribution1}).
}
\description{
The probability that a distribution falls to one side of a value.
\code{eval_prob_left()} is the cdf and \code{eval_prob_right()} the survival
function, named for the direction rather than the convention, and free to
take either inequality.
}
\details{
These are the same probabilities \code{\link[=eval_cdf]{eval_cdf()}} and \code{\link[=eval_survival]{eval_survival()}} give,
under names that say which way they point. Which pair to reach for is a
matter of what the code around them is about: a survival analysis is
written in survival functions, while a question about whether a value
falls short of a threshold reads better as the probability to its left.
}
\examples{
d <- dst_pois(5)
eval_prob_left(d, at = 3)
eval_prob_left(d, at = 3, inequality = "strict")
eval_prob_right(d, at = 0:3, inequality = "weak")
enframe_prob_left(d, at = 0:3)
}
\seealso{
Other distributional representations: 
\code{\link{eval_cdf}()},
\code{\link{eval_chf}()},
\code{\link{eval_density}()},
\code{\link{eval_hazard}()},
\code{\link{eval_odds}()},
\code{\link{eval_pmf}()},
\code{\link{eval_quantile}()},
\code{\link{eval_return}()},
\code{\link{eval_survival}()}
}
\concept{distributional representations}
