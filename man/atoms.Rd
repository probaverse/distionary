% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support.R
\name{atoms}
\alias{atoms}
\alias{regions}
\title{What a Support Is Made Of}
\usage{
atoms(x)

regions(x)
}
\arguments{
\item{x}{A support object or a distribution.}
}
\value{
For \code{atoms()}, a \code{discretes} object. For \code{regions()}, a two-column
numeric matrix of intervals (\code{lower}, \code{upper}), one row each.
}
\description{
Take a support apart: \code{atoms()} gives the points it places mass on, and
\code{regions()} gives the intervals it spreads mass across. Each accepts a
support object or a distribution.
}
\details{
These are the inverses of the constructors. \code{discrete()} builds a support
out of atoms and \code{atoms()} gives them back; \code{continuous()} builds one out of
regions and \code{regions()} gives those back. So the discrete part of a support
is \code{discrete(atoms(x))}, and its continuous part is \code{continuous(regions(x))}.

A support with no atoms gives an empty \code{discretes} object rather than
nothing, and one with no regions gives a matrix of no rows, so neither has
to be guarded against before being used. That is a definite answer: the
support says there is no part of that kind.

\code{\link[=dst_null]{dst_null()}} is different. It has no support at all, so there is nothing to
take apart and nothing is known --- both give \code{NULL}, as \code{\link[=support]{support()}} does
for it, rather than claiming it has no atoms. This mirrors \code{\link[=range]{range()}}, which
answers \code{c(NA, NA)} for it instead of refusing.
}
\examples{
atoms(mixed(discrete = 0, continuous = c(0, Inf)))
regions(continuous(c(0, 1), c(3, 4)))

# Either part can be put back together into a support of its own.
s <- mixed(discrete = c(0, 5), continuous = c(0, 10))
discrete(atoms(s))
continuous(regions(s))
}
\seealso{
Other Support: 
\code{\link{empty_support}()},
\code{\link{is_support}()},
\code{\link{support}()},
\code{\link{support-construction}}
}
\concept{Support}
