% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dst_null.R
\name{dst_null}
\alias{dst_null}
\title{Null Distribution}
\usage{
dst_null()
}
\value{
A Null distribution.
}
\description{
Sometimes it's convenient to work with a distribution object that is
akin to a missing value. This is especially true when programmatically
outputting distributions, such as when a distribution fails to fit to
data. This function makes such a distribution object. It always evaluates
to \code{NA}.
}
\details{
The Null distribution is the missing value of the distribution world, and
every query about it answers \code{NA} in whatever type that query returns:
\code{NA_real_} from \code{\link[=mean]{mean()}} and the \verb{eval_*()} functions, \code{NA_character_} from
\code{\link[=vtype]{vtype()}}, \code{c(NA, NA)} from \code{\link[=range]{range()}}, and no support at all --- \code{\link[=support]{support()}}
returns \code{NULL}, R's absent-object value. It is also the one distribution
that \code{\link[=is.na]{is.na()}} finds; see \code{\link[=length.dst]{length.dst()}}.

Because of that it is assembled with the package's low-level constructor
rather than through \code{\link[=distribution]{distribution()}}. A Null distribution cannot satisfy
what \code{\link[=distribution]{distribution()}} asks of a real one, since it has nothing to declare;
building it here keeps that bypass internal, so a distribution with no
support cannot be made through the front door.
}
\examples{
x <- dst_null()
mean(x)
eval_pmf(x, at = 1:10)

# It is the distribution that `is.na()` finds.
is.na(x)
is.na(dst_norm(0, 1))

# Everything about it is missing, including its support.
vtype(x)
range(x)
support(x)
}
