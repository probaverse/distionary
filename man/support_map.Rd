% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-map.R
\name{support_map}
\alias{support_map}
\alias{support_affine}
\title{A Support as the Image of Another}
\usage{
support_map(support, fun, ..., margins)

support_affine(support, ..., shift, matrix)
}
\arguments{
\item{support}{The support being mapped: univariate, or a product of
univariate supports.}

\item{fun}{For \code{support_map()}, the map. It takes one vector per variable
of \code{support} (as the representations of a multivariate distribution do)
and returns a list of vectors, one per output variable.}

\item{...}{Not used; forces the other arguments to be named.}

\item{margins}{For \code{support_map()}, a list of univariate supports, one per
output variable: the values each can take. They cannot, in general, be
worked out from \code{fun}, so they are stated. Their names name the variables.}

\item{shift, matrix}{For \code{support_affine()}, the map
\eqn{u \mapsto \mathrm{shift} + \mathrm{matrix} \, u}. \code{matrix} has one row
per output variable and one column per variable of \code{support}. The names
of \code{shift}, or the row names of \code{matrix}, name the variables.}
}
\value{
A support object.
}
\description{
Describe a support as where a map sends a simpler one. This is how to
describe supports that are not products, such as a triangle (\code{y <= x}) or
a line (\code{x + y = 100}): each is the image of a product under a map.
\code{support_affine()} is for linear maps, where everything about the image
can be worked out exactly; \code{support_map()} is for any map, and needs to be
told what cannot be worked out.
}
\details{
\subsection{Why describe a support by a map}{

The alternative is a product with restrictions laid on it: all of
\eqn{(x, y)}, subject to \eqn{y \le x}. That is easy to state, but a
restriction can only answer whether a point belongs; it cannot say what
values one variable reaches, or how many dimensions the support spans.
A map says both. The support of \eqn{(X, XZ)}, for \eqn{X > 0} and
\eqn{0 < Z < 1}, is the image of a rectangle under
\eqn{(x, z) \mapsto (x, xz)}; the line \eqn{x + y = 100} is the image of
the real line under \eqn{t \mapsto (50 + t, 50 - t)}.
}

\subsection{Dimension and variable type}{

The support has one variable per output of the map (\code{\link[=dimension]{dimension()}}), but
may span fewer dimensions than that: a line in the plane spans one. When
it spans fewer, its variable type (\code{\link[=vtype]{vtype()}}) is \code{"singular"}. There is
then no density with respect to area (or volume), even though no single
point carries any probability. Take a \code{\link[=marginal]{marginal()}} to get back a
distribution with a density.

For \code{support_affine()}, the number of dimensions spanned is the rank of
\code{matrix}. For \code{support_map()}, it is taken to be the smaller of the
number of variables going in and coming out.
}

\subsection{Restrictions}{

\code{support_affine()} works out each variable's support exactly, which needs
\code{support} to be continuous: for a finite set of points it gives the
mapped points directly, and anything else with atoms is not supported
yet.
}
}
\examples{
# The line x + y = 100, as the image of the real line.
line <- support_affine(
  continuous(),
  shift = c(x = 50, y = 50),
  matrix = rbind(1, -1)
)
line
dimension(line)

# The triangle 0 < y < x, as the image of (x, z) -> (x, x * z).
support_map(
  support_product(x = continuous(c(0, Inf)), z = continuous(c(0, 1))),
  fun = function(x, z) list(x, x * z),
  margins = list(x = continuous(c(0, Inf)), y = continuous(c(0, Inf)))
)
}
\seealso{
\code{\link[=support_product]{support_product()}} for supports that are products.

Other Support: 
\code{\link{atoms}()},
\code{\link{empty_support}()},
\code{\link{is_support}()},
\code{\link{support}()},
\code{\link{support-construction}},
\code{\link{support_product}()}
}
\concept{Support}
