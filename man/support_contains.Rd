% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support-algebra.R
\name{support_contains}
\alias{support_contains}
\alias{support_has_atom}
\title{Test Membership of a Support}
\usage{
support_contains(support, at)

support_has_atom(support, at)
}
\arguments{
\item{support}{A support object, or a distribution.}

\item{at}{Values to test. Vectorised.}
}
\value{
A logical vector the same length as \code{at}.
}
\description{
Is a value in the support at all, or an atom of it specifically?
}
\details{
\code{support_contains()} is \code{TRUE} for a value that is either an atom or inside
one of the continuous intervals. \code{support_has_atom()} is \code{TRUE} only for the
atoms, and so is the one to reach for when what matters is whether a point
carries positive probability.

Continuous intervals count their endpoints as contained. Those endpoints
carry no probability, so a value can be contained in a support without being
a point of positive mass --- which is exactly the distinction between these
two functions.
}
\examples{
s <- mixed(atoms = 0, continuous = c(2, 5))
support_contains(s, at = c(0, 1, 3, 9))
support_has_atom(s, at = c(0, 1, 3, 9))

support_has_atom(dst_pois(3), at = c(-1, 0, 2.5, 4))
}
\seealso{
Other Support algebra: 
\code{\link{support_add_atoms}()},
\code{\link{support_restrict}()},
\code{\link{support_transform}()},
\code{\link{support_union}()}
}
\concept{Support algebra}
